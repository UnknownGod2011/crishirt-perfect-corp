# Quick Deploy

Use this for the Perfect Corp version of the app.

Backend start command:

```bash
node backend/index.perfect.js
```

Frontend build command:

```bash
npm install
npm run build
```

Set the Perfect Corp variables from `.env.example` in the host secret manager. Keep `PERFECT_DEMO_MODE=false` for the judging demo.
